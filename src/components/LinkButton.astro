---
interface Props {
	href: string;
	title: string;
}

const { title, href } = Astro.props;
---

<a
	href={href}
	class="px-2 py-4 w-96 bg-secondary text-4xl text-primary text-center rounded shadow-3xl hover:bg-[#B3B770]"
	title={title}>{title}</a
>
